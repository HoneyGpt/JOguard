import { BlockRule } from '../types';

/**
 * User Custom Rules & Extensible Remote Filter Rules
 */
export const DEFAULT_CUSTOM_RULES: BlockRule[] = [
  // Extensible container for user custom rules created in Options page
];
